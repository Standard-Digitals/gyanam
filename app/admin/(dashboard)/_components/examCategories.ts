// Shared exam category list — kept identical across Courses, Mock Tests and Quizzes
// so the same options show up everywhere in the admin dashboard.
export const EXAM_CATEGORIES = ['SSC', 'Banking', 'Railway', 'UPSC', 'Assam Govt', 'State PSC', 'Defence'];
